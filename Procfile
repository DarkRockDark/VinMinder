#Make sure that the Procfile is using the correct syntax, and that there are no extra tabs or newlines in the file.
web: java -Dspring.profiles.active=mlab -jar build/libs/vinMinder-1.0-SNAPSHOT.war
# web: java -Dserver.port=$PORT -jar target/*.jar